#include "htmlparser.h"
#include "server.h"

#include <iostream>
using namespace std;
int main(int argc, char* argv[]) {
  cout << "Server is running ... just kidding" << endl;
  example_method(0);
  return 0;
}
